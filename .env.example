# Data Source Configuration
# Choose which data source to use for user management: 'postgres', 'mongodb', or 'json'
DATA_SOURCE=json

# PostgreSQL Configuration (if using postgres)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mcp_users
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# MongoDB Configuration (if using mongodb)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=mcp_users

# Slack Integration (optional - for search features)
SLACK_TOKEN=xoxb-your-slack-bot-token

# Google API Configuration (optional - for Google Docs and Web search)
GOOGLE_API_KEY=your-google-api-key
GOOGLE_SEARCH_ENGINE_ID=your-custom-search-engine-id
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"your-project"}

# Database for Search Feature (optional)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mcp_users
DB_USER=postgres
DB_PASSWORD=password
